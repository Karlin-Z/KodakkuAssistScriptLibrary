<!--
提交前请确认：
1. 本 PR 只改动了以你的 GitHub 用户名命名的文件夹；
2. 只新增 / 修改 / 删除了 .cs 文件；
3. 每个 .cs 里有且只有一处 [ScriptType(...)]，且 guid 唯一；
4. 已本地运行过：node .github/scripts/pr_review.mjs --owner <你的用户名> --check-cs <你的文件夹>/<脚本>.cs
校验通过后机器人会自动 squash 合并，无需手动操作。
-->

## 改动说明

<!-- 简要说明这次改动：新增脚本 / 更新版本 / 修 bug 等 -->

## 自检

- [ ] 文件夹名与我的 GitHub 用户名一致
- [ ] 只改动了 .cs 文件
- [ ] 每个 .cs 里只有一处 `[ScriptType(...)]`
